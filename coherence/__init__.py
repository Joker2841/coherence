"""Coherence — the integrity layer for AI memory.

A contradiction / belief-consistency engine built on top of Cognee's memory
lifecycle. We extend Cognee where it is designed to be extended (a custom
memify pipeline) rather than fighting the framework.
"""

__version__ = "0.1.0"
